output: build/dist/

releases:
  - name: release
    jobs:
      - name: package-linux-deb
        package:
          platform: linux
          target: deb
      - name: package-linux-rpm
        package:
          platform: linux
          target: rpm